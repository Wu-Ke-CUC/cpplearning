#include<algorithm>
#include<iostream>
#include<vector>
using namespace std;
bool cmp(int n, int m)
{
	return n > m;
}
int main()
{
	/*int total, n;
	cin >> total >> n;
	int* money = new int[n];
	for (int i = 0; i < n; i++)
	{
		cin >> money[i];
	}
	sort(money, money + n,cmp);
	int ret = 0;
	for (int i = 0; i < n; i++)
	{
		if (money[i] <= total)
		{
			int num = total / money[i];
			total -= num * money[i];
			ret += num;
		}
	}
	cout << ret << endl;*/
	return 0;
}
